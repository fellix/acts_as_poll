class Create<%= camel_name %>Options < ActiveRecord::Migration

	def self.up
		create_table :<%= model_name %>_options do |t|
			t.references :<%= model_name %>
			t.string :content
			t.integer :num_resp, :default => 0
		end
	end
	
	def self.down
		drop_table :<%= model_name %>_options
	end

end
