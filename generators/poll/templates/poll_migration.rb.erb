class Create<%= plural_name.camelize %> < ActiveRecord::Migration
	def self.up
		create_table :<%= plural_name %> do |t|
			t.text :description
			t.boolean :show_result, :default => true
			t.string :mtype, :limit => 1, :default => "R"
		end
	end
	
	def self.down
		drop_table :<%= plural_name %>
	end
end
